# Keep in sync with pyproject.toml [project.dependencies].
# Starlette is a transitive dependency of FastAPI — do not pin it here
# (a hard pin like starlette==1.0.1 conflicts with modern fastapi>=0.110).
x402-conformance-suite>=0.5.2
fastapi>=0.110
uvicorn[standard]>=0.27
flask>=3.0
aiohttp>=3.9
pyyaml>=6.0
stripe>=8.0
pydantic>=2.0
psycopg[binary,pool]>=3.2
httpx>=0.27
argon2-cffi>=23.1
python-multipart>=0.0.9
