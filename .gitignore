__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.venv/
venv/
.env
.coverage
.pytest_cache/
dashboard/data/
node_modules/

# Local-only: contains account IDs and operational details (see vault)
CLAUDE.md

# Claude Code / agent session exports (may contain opsec detail; never commit)
*-this-session-is-being-continued-from-a-previous-*.txt
*.session-export.txt

# secret-shuttle env-ref files (refs only, never secrets — kept local anyway)
*.refs

# Facilitator gas probe — real events / reports (never commit)
data/facilitator_events.jsonl
data/facilitator_merged.jsonl
reports/*.json
reports/*.csv
# examples/facilitator_events.sample.jsonl is synthetic and may be committed
